# Add project-specific ProGuard rules here.
# Minification is disabled for the v1 release build (isMinifyEnabled = false),
# so no custom keep rules are required yet. Add them here when R8 is enabled.
#
# For more details, see
#   http://developer.android.com/guide/developing/tools/proguard.html
